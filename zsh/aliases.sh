#!/usr/bin/env bash

alias reload='exec ${SHELL}'
alias tf="terraform"

alias sshnick="ssh -i ${HOME}/.ssh/nick"
alias sshltel="ssh -i ${HOME}/.ssh/ltel"

alias sshdev="ssh -i ${HOME}/.ssh/cds-dev"
alias sshtest="ssh -i ${HOME}/.ssh/cds-test"
alias sshprod="ssh -i ${HOME}/.ssh/cds-prod"
